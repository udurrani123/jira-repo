name: Jest Tests
on: 
  repository_dispatch:
    types: [run-jest-tests]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm install
      - run: npm test
